# Project Tasks & Roadmap

## Task 1: Account Quota Caching & Dedicated Refresh System

### Context & Goal
বর্তমানে `/api/state` এন্ডপয়েন্টটি প্রতিবার কল হলে একই সাথে লোকাল PC স্ট্যাটাস (ব্যাটারি, চার্জিং, স্লিপ ইনহিবিটর ইত্যাদি) এবং ৬টি অ্যাকাউন্টের লাইভ কোটা ফাইল রিড করে। 
- PC স্ট্যাটাস (যেমন ব্যাটারি ও চার্জিং) ঘন ঘন (৩০-৪৫ সেকেন্ড পরপর) লাইভ জানা দরকার।
- কিন্তু অ্যাকাউন্টের কোটা/লিমিট ঘন ঘন চেক করার প্রয়োজন নেই। এটার জন্য ৩ মিনিটের ক্যাশিং এবং ব্যবহারকারীর প্রয়োজনে ইনস্ট্যান্ট ম্যানুয়াল রিফ্রেশ বাটন থাকা বেশি কার্যকর ও অপটিমাইজড।

### Planned Implementation Details

1. **Backend Quota In-Memory Cache (Inside `/api/state`)**
   - [x] `/api/state` হ্যান্ডলারে ৩ মিনিটের (`180s`) ইন-মেমোরি কোটা ক্যাশ মেকানিজম যুক্ত করা।
   - [x] পিসি স্ট্যাটাস (ব্যাটারি %, চার্জিং স্ট্যাটাস, স্লিপ মোড, IDE রানিং স্টেট) প্রতি রিকোয়েস্টে রিয়েলটাইম আপডেট থাকবে।
   - [x] অ্যাকাউন্ট কোটা ডেটা প্রতি ৩ মিনিটে একবার রিড হবে, বাকি সময় ক্যাশ থেকে ইনস্ট্যান্ট সার্ভ হবে।
   - [x] `?refresh_quota=1` বা `force_refresh=true` কোয়েরি প্যারামিটার সাপোর্ট রাখা, যাতে ম্যানুয়াল রিকোয়েস্টে সাথে সাথে ক্যাশ বাইপাস করে ফ্রেশ ডেটা ফেচ করা যায়।

2. **Dedicated Quota Refresh Button (Frontend UI)**
   - [x] অ্যাকাউন্ট লিস্ট / কোটা সেকশনে একটি স্লিক রিফ্রেশ আইকন বাটন যোগ করা।
   - [x] বাটনে ক্লিক করলে `?refresh_quota=1` সহ ব্যাকএন্ড কল হবে এবং আইকন স্পিন অ্যানিমেশন দিয়ে ইনস্ট্যান্ট ফিডব্যাক দেবে।
   - [x] ৩-৫ সেকেন্ডের অ্যান্টি-স্প্যাম কুলডাউন রাখা যাতে দ্রুত পরপর ক্লিকে সার্ভারে লোড না পড়ে।

3. **Frontend Polling Optimization**
   - [x] ড্যাশবোর্ডের রেগুলার পোলিং স্বাভাবিক রাখা (যাতে পিসি অনলাইন ও চার্জিং দ্রুত জানা যায়), তবে ব্যাকএন্ড ক্যাশের কারণে অ্যাকাউন্টের ভারী প্রসেসিং বাইপাস হবে।

4. **PC Online & Battery UI Polish (/impeccable)**
   - [x] ছোট মোবাইল ভিউপোর্টে 'PC Online' ও ব্যাটারি ব্যাজের স্পেসিং, ফন্ট এবং চার্জিং বোল্ট অ্যানিমেশন আরও নিখুঁত ও ব্যালান্সড করা।

---

## Task 2: Mobile Web Terminal Efficiency, IME & Typing Overhaul

### Deep Research & Root Cause Breakdown

মোবাইল ব্রাউজারে (Android Gboard, Samsung Keyboard, iOS Safari/Chrome) `xterm.js` চালানোর সময় ডেভেলপার ও ব্যবহারকারীরা যে প্রধান সমস্যাগুলোর মুখোমুখি হন:

1. **IME Composition ও Word Duplication (`cd .` -> `cd cd.` বা `ce..`)**
   - *রুট কজ:* মোবাইল ভার্চুয়াল কীবোর্ড টেক্সট টাইপ করার সময় শব্দগুলোকে ব্রাউজারের দৃশ্যমান/অদৃশ্য `<textarea>`-তে একটি "প্রি-এডিট / কম্পোজিশন বাফারে" আটকে রাখে। ইউজার যখন কোনো সিম্বল (যেমন ডট `.`, স্পেস বা ব্যাকস্পেস) দেয়, তখন কীবোর্ড পুরো বাফারটি একবারে রিলিজ (commit) করে। ফলে xterm আগের লেখা টেক্সটটি আবার পায় এবং স্ক্রিনে টেক্সট ডুপ্লিকেট হয়ে যায়।
   - *কার্সার জাম্পিং:* `_syncTextArea` যখন কীবোর্ডের বাফার পরিবর্তন দেখে কার্সার পজিশন মেলাতে যায়, ব্রাউজার নিজে থেকেই সিলেকশনকে শব্দের শুরুতে বা শেষে জোরপূর্বক ঠেলে দেয়।

2. **ব্যাকস্পেস (Backspace) ডিলিট না হওয়া বা হ্যাং হওয়া**
   - *রুট কজ:* Gboard যখন কোনো কম্পোজিশন বা সাজেশনের ভেতরে থাকে, তখন Backspace চাপলে স্ট্যান্ডার্ড `keyCode: 8` বা ASCII `0x08`/`0x7f` না পাঠিয়ে `inputType: "deleteContentBackward"` পাঠায়। xterm.js এর ডিফল্ট হ্যান্ডলার অনেক সময় এটি শেলে রিলে করতে পারে না।

3. **স্মার্ট পাঙ্কচুয়েশন (Smart Punctuation & Quotes Corrupting Commands)**
   - *রুট কজ:* মোবাইল কীবোর্ড স্বয়ংক্রিয়ভাবে ডাবল ড্যাশ `--` কে এম-ড্যাশ `—` এ রূপান্তর করে (যেমন: `--help` হয়ে যায় `—help`)। এছাড়া সোজা কোট (`'`, `"`) কে কার্লি কোট (`‘`, `’`, `“`, `”`) বানিয়ে দেয়। লিনাক্স শেলে এগুলো যাওয়া মাত্র `command not found` বা `syntax error` ঘটে।

4. **অটো-ক্যাপিটালাইজেশন (First Letter Capitalization)**
   - *রুট কজ:* মোবাইলের `autocapitalize` ডিফল্টভাবে নতুন লাইনে প্রথম অক্ষর বড় হাতের করে ফেলে। ফলে `git status` টাইপ করতে গেলে `Git status` হয়ে যায় এবং কমান্ড ফেইল করে।

5. **টাচ স্ক্রিনে টেক্সট সিলেকশন, কপি ও পেস্ট (No Touch Selection Handles)**
   - *রুট কজ:* `xterm.js`-এ মোবাইলের জন্য কোনো নেটিভ টাচ সিলেকশন পিন বা হ্যান্ডেল নেই। স্ক্রিনে লং-প্রেস করলে ব্রাউজারের নেটিভ কনটেক্সট মেনু বা জুম ট্রিগার হয়, যা টার্মিনালের মাউস ইভেন্টের সাথে কনফ্লিক্ট করে। ফলে কমান্ড কপি বা আউটপুট সিলেক্ট করা দুঃসাধ্য হয়ে পড়ে।

6. **ভার্চুয়াল কীবোর্ড ভিউপোর্ট রিসাইজ ও লেআউট ট্র্যাশিং (`visualViewport` & SIGWINCH)**
   - *রুট কজ:* মোবাইলে কীবোর্ড ওপেন হলে `window.visualViewport` হঠাৎ ছোট হয়ে যায়। যদি প্রতি ফ্রেমে `fitAddon.fit()` কল হয়, তবে হোস্ট সার্ভারের PTY-তে ঘন ঘন `SIGWINCH` রিসাইজ সিগন্যাল যায়। এতে টার্মিনাল প্রম্পট বারবার রিড্র হয়, লাইন ভেঙে যায় এবং স্ক্রিন ফ্লিকার করে। তাছাড়া কীবোর্ড অনেক সময় ইনপুট কার্সারের লাইনটি ঢেকে ফেলে।

7. **অ্যাক্সিডেন্টাল ডাবল-ট্যাপ ও পিঞ্চ জুম (Pinch-to-Zoom Misalignment)**
   - *রুট কজ:* টার্মিনাল ক্যানভাসে দ্রুত দুইবার ট্যাপ বা স্ক্রোল করার সময় ভুলবশত জুম হয়ে গেলে xterm ক্যানভাসের গ্রিড কোঅর্ডিনেট ভেঙে যায়।

---

### Planned Implementation Details for Task 2

1. **Mobile Helper `<textarea>` হার্ডেনিং (Predictive & Autocorrect Neutralizer)**
   - [x] টার্মিনালের ইন্টারনাল টেক্সট-এরিয়া ডিটেক্ট করে কঠোর মোবাইল অ্যাট্রিবিউট প্রয়োগ করা:
     `autocorrect="off"`, `autocapitalize="none"`, `autocomplete="off"`, `spellcheck="false"`, `enterkeyhint="go"`।
   - [x] প্রয়োজনে `inputmode="email"` বা `inputmode="search"` টেস্ট করা যা কীবোর্ডকে বাধ্য করে প্রেডিকশন ইঞ্জিন বন্ধ রেখে র' ক্যারেক্টার সেন্ড করতে।

2. **ইনপুট ও কম্পোজিশন গার্ড (IME Duplication & Backspace Handler)**
   - [x] `beforeinput` ইভেন্টে `deleteContentBackward` কে সরাসরি লিনাক্স ASCII Backspace (`\x7f`) এ বাইন্ড করা।
   - [x] স্মার্ট পাঙ্কচুয়েশন ক্লিনার: যদি কোনোভাবে কীবোর্ড কার্লি কোট (`“`, `”`, `‘`, `’`) বা এম-ড্যাশ (`—`) পাঠায়, তাৎক্ষণিকভাবে সেগুলোকে সোজা কোট (`"`, `'`) এবং নরমাল ড্যাশ (`--`) এ স্যানিটাইজ করে শেলে পাঠানো।

3. **ক্লিপবোর্ড ও টাচ এক্সেসরিজ (Mobile Copy & Paste Engine)**
   - [x] **ইনস্ট্যান্ট পেস্ট বাটন (`📋 Paste`):** এক্সেসরি বারে একটি ডেডিকেটেড পেস্ট বাটন যোগ করা যা `navigator.clipboard.readText()` দিয়ে এক ক্লিকে ক্লিপবোর্ডের টেক্সট শেলে ইনসার্ট করবে।
   - [x] **কপি বাটন / কুইক সিলেকশন টুল (`📑 Copy Output`):** টার্মিনালের সিলেক্টেড টেক্সট বা কারেন্ট প্রম্পটের আউটপুট সহজে ক্লিপবোর্ডে কপি করার ওয়ান-ট্যাপ বাটন।
   - [x] **এক্সেসরি বারে ব্যাকস্পেস বাটন (`⌫ Del`):** কীবোর্ড বাফারিং সমস্যায় পড়লেও যেন ইউজার সরাসরি নির্ভরযোগ্যভাবে অক্ষর মুছতে পারে।

4. **স্মুথ ভিউপোর্ট ও কীবোর্ড ম্যানেজমেন্ট (`visualViewport` Debounced Resizing)**
   - [x] `window.visualViewport` লিসেনার যুক্ত করে কীবোর্ড ওপেন/ক্লোজ ট্র্যাক করা।
   - [x] কীবোর্ড অ্যানিমেশন চলাকালীন র্যাপিড `fit()` ঠেকানোর জন্য ২০০ms ডিবাইউন্স (Debounce) যোগ করা যাতে হোস্টে অপ্রয়োজনীয় `SIGWINCH` স্প্যাম না হয়।
   - [x] কীবোর্ড খোলার পর অ্যাক্টিভ টার্মিনাল কার্সারকে ভিউপোর্টের দৃশ্যমান অংশে অটো-স্ক্রোল করে রাখা।

5. **মোবাইল জেসচার গার্ড**
   - [x] টার্মিনাল কন্টেইনারে `touch-action: pan-y` এবং মেটা ভিউপোর্টে `user-scalable=no` বা `maximum-scale=1.0` প্রয়োগ করে আনওয়ান্টেড জুম বা ক্যানভাস ডিসিঙ্ক প্রতিরোধ করা।

---

## Task 3: Mobile Terminal Benchmarking (zalo/terminal & TermBeam Integrations)

### Implemented Ergonomics & Architecture

1. **Select Mode Overlay (Native Mobile Touch Selection & Copy All)**
   - [x] টার্মিনালে টেক্সট সিলেক্টেড থাকলে `📑` বাটন সরাসরি কপি করে; কিন্তু আন-সিলেক্টেড অবস্থায় বা টার্মিনালে লং-প্রেস (550ms) করলে সরাসরি নেটিভ **Select Mode Overlay** ওপেন হয়।
   - [x] মোবাইল ব্রাউজারের নেটিভ টাচ সিলেকশন হ্যান্ডেল (পিন/ম্যাগনিফায়ার) দিয়ে যেকোনো শব্দ বা লাইন সিলেক্ট ও কপি করা যায়।
   - [x] 'Copy Selection', 'Copy All', এবং 'Done' বাটন সহ ক্লাউড/লোকাল ক্লিপবোর্ড সিঙ্ক ও ফলব্যাক ব্যবস্থা।

2. **Soft Keyboard Show/Hide Toggle (`⌨`)**
   - [x] হেডার এবং দ্রুত অ্যাক্সেসের জন্য ভার্চুয়াল কীবোর্ড ব্লার/ফোকাস টগল বাটন।
   - [x] স্ক্রিন থেকে কীবোর্ড সরিয়ে পুরো টার্মিনাল আউটপুট দেখা এবং যেকোনো সময় এক ট্যাপে কীবোর্ড ফিরিয়ে আনার সুবিধা।

3. **Command Composer / Dictation Drawer (`✏️`)**
   - [x] হেডার থেকে স্লাইড-ডাউন টেক্সট এরিয়া যেখানে মোবাইলের ভয়েস টাইপিং (Speech-to-Text), জিবোর্ড ডিকটেশন ও অটো-কারেক্ট স্বাধীনভাবে কাজ করে।
   - [x] 'Send ⏎' চাপলে কমান্ডটি সরাসরি শেলে এক্সিকিউট হয়।

4. **Alt Modifier & Key Chord Ergonomics**
   - [x] এক্সেসরি বারে স্টিকি `ALT` বাটন যোগ করা (যেমন Alt+F, Alt+B, Alt+D, Alt+. এর জন্য `\x1b` প্রিফিক্স হ্যান্ডলিং)।
   - [x] অ্যাক্টিভ অবস্থায় ভিজ্যুয়াল গ্লো এবং ব্যবহারের পর অটো-রিলিজ।

5. **Alt-Screen Swipe Gesture & Floating Scroll Button (`↓`)**
   - [x] `htop`, `vim`, `nano`, `less` এর মতো অল্টারনেট স্ক্রিন বাফারে স্ক্রিন সোয়াইপ করলে স্বয়ংক্রিয়ভাবে Up/Down তীর চিহ্নে রূপান্তর।
   - [x] স্ক্রোল ব্যাক করার সময় নিচে ভাসমান `↓` বাটন যা কীবোর্ড না পপ-আপ করে স্মুথলি বটমে স্ক্রোল করায়।

6. **Tactile Haptic Feedback & Safe-Area Ergonomics**
   - [x] সমস্ত এক্সেসরি বাটন এবং কুইক কমান্ড চিপে হালকা ভাইব্রেশন (`navigator.vibrate(12)`).
   - [x] আইফোন ও জেসচার বার সাপোর্টের জন্য `viewport-fit=cover` ও CSS `env(safe-area-inset-...)` নিশ্চিত করা।

---

## Task 4: Complete Web Terminal Protocol, Session Persistence, IME & Security Overhaul (31-Point Audit)

### 1. Critical Architecture & Reliability (Audit 1–7)
- [x] **RFC 6455 Binary Frame Streaming (Opcode 2):** PTY আউটপুট সরাসরি বাইনারি ফ্রেমে স্ট্রিম করা হয়। ফলে বাংলা, ইমোজি, বা TUI বক্স-ড্রয়িং ক্যারেক্টার ৪KB রিড বাউন্ডারিতে কাটলেও ব্রাউজারে RFC 6455 কোড 1007 ডিসকানেক্ট হয় না।
- [x] **Background Shell & PTY Persistence (Session Manager):** পিওর পাইথন স্ট্যান্ডার্ড লাইব্রেরিতে `PTYSessionManager` ও ১২৮KB রিং বাফার যোগ করা হয়েছে। মোবাইল নেটওয়ার্ক ড্রপ, ট্যাব সুইচ, ব্যাকগ্রাউন্ড বা স্ক্রিন লক হলেও হোস্টের শেল প্রসেস ব্যাকগ্রাউন্ডে নিরাপদে চলতে থাকে। রিকানেক্টে আগের সম্পূর্ণ স্ক্রিন ব্যাকলগ রিপ্লে হয়।
- [x] **Inherited Python Signal Reset:** চাইল্ড শেল প্রসেস স্পন করার পূর্বে পাইথনের ডিফল্ট `SIGPIPE = SIG_IGN` পরিবর্তন করে `SIG_DFL` এ রিস্টোর এবং `pthread_sigmask` আনমাস্ক করা হয়েছে। ফলে পাইপ কমান্ড (`cat | head`) ভাঙবে না।
- [x] **Slave PTY `termios.IUTF8` Flag:** স্লেভ টার্মিনাল লাইন ডিসিপ্লিনে `termios.IUTF8` সেট করা হয়েছে, যা ক্যানোনিকাল মোডে মাল্টিবাইট ক্যারেক্টারের সঠিক ব্যাকস্পেস হ্যান্ডলিং নিশ্চিত করে।
- [x] **Separation of Control vs Terminal Data:** টেক্সট ফ্রেম (Opcode 1) শুধুমাত্র JSON কন্ট্রোল কমান্ডের (যেমন `resize`, `ping`) জন্য ব্যবহৃত হয় এবং বাইনারি ফ্রেম (Opcode 2) র' টার্মিনাল ডেটার জন্য ব্যবহৃত হয়। ফলে শেলের আউটপুটে বা পেস্ট করা টেক্সটে `{"type":"pong"}` বা `{"type":"resize"}` থাকলেও কোনো কমান্ড ইনজেকশন বা ড্রপ হবে না।
- [x] **WebSocket Continuation Frame (Opcode 0) Reassembly:** ফ্র্যাগমেন্টেড ওয়েবসকেট ফ্রেম পূর্ণাঙ্গভাবে রিসিভ করে জোড়া লাগিয়ে PTY-তে পাঠানো হয়।
- [x] **Process Group Teardown:** সেশন সম্পূর্ণ ক্লোজ করার সময় `os.killpg(child_pid, ...)` দিয়ে অরফান ব্যাকগ্রাউন্ড প্রসেস ক্লিনআপ নিশ্চিত করা হয়েছে।

### 2. Mobile IME & Input Ergonomics (Audit 8–15)
- [x] **Desktop Mouse SGR Support:** ডেস্কটপ মাউস ইভেন্ট (`\x1b[<...M`) সার্ভার থেকে গ্লোবালি ফিল্টার না করে অক্ষুণ্ণ রাখা হয়েছে; টাচ ডিভাইসে সিন্থেটিক মাউস ফিল্টারিং করা হয় শুধুমাত্র সাম্প্রতিক টাচের সময়ের ভিত্তিতে (`Date.now() - lastTouchTime < 650`)। ফলে ডেস্কটপে `vim`, `htop`, `tmux` মাউস ইন্টারঅ্যাকশন ১০০% কার্যকর।
- [x] **Gboard Autocorrect & IME Hardening:** `beforeinput` ইভেন্টে `insertReplacementText` এবং কম্পোজিশন ট্র্যাকিং (`isComposing`) যুক্ত করে মোবাইলে `cd .` দিলে `cd cd.` বা `ce..` হওয়ার ডুপ্লিকেশন সমস্যা গোড়া থেকে সমাধান করা হয়েছে।
- [x] **Smart Punctuation Sanitization:** মোবাইল কীবোর্ডের বাঁকা কোট (`“`, `”`, `‘`, `’`) ও এম-ড্যাশ (`—`) স্বয়ংক্রিয়ভাবে স্ট্রেট কোট (`"`, `'`) ও হাইফেন (`--`) এ রূপান্তর।
- [x] **Bracketed Paste Mode:** ক্লিপবোর্ড পেস্ট বা কম্পোজার ড্রয়ার থেকে ইনপুট দেওয়ার সময় `term.paste(text)` ব্যবহার করা হয়েছে, যাতে শেলে স্বয়ংক্রিয়ভাবে এক্সিকিউট না হয়ে প্রম্পটে প্রটেক্টেড থাকে।
- [x] **DECCKM Application Cursor Mode Support:** অ্যারো কী প্রেরণের সময় টার্মিনালের `applicationCursorKeysMode` যাচাই করে `\x1bOA` বনাম `\x1b[A` যথাযথভাবে পাঠানো হয়।
- [x] **Sticky Modifiers Visual Feedback & Auto-Release:** Ctrl ও Alt মডিফায়ার অ্যাক্টিভেশনের পর ভিজ্যুয়াল স্টেট এবং একটি ক্যারেক্টার ব্যবহারের পর অটো-রিলিজ।
- [x] **Font Loading Synchronization:** ফন্ট সম্পূর্ণরূপে লোড হওয়ার পর (`document.fonts.ready`) টার্মিনালের ডাইমেনশন ফিট করা হয়।

### 3. Viewport & Connection Polish (Audit 16–27)
- [x] **Soft Keyboard Focus Preservation:** এক্সেসরি বারের বাটনগুলোতে `pointerdown` এ `e.preventDefault()` প্রয়োগ করে ভার্চুয়াল কীবোর্ড ব্লার হওয়া বন্ধ করা হয়েছে।
- [x] **iOS Visual Viewport Anchoring:** আইওএস সাফারি ও অ্যান্ড্রয়েড ক্রোম ভিউপোর্টে কীবোর্ডের উচ্চতা ডাইনামিকালি `window.visualViewport.height` এ অ্যাঙ্কর করা।
- [x] **Resize Debouncing:** ২০০ms ডিবাইউন্স দিয়ে উইন্ডো বা কীবোর্ড পরিবর্তনের সময় অতিরিক্ত `SIGWINCH` স্প্যাম বন্ধ করা হয়েছে।
- [x] **Initial Size Sync:** ওয়েবসকেট ওপেন হওয়ার পরপরই বর্তমান কলাম ও র' ব্যাকএন্ডে সিঙ্ক করা।
- [x] **Half-Open Connection Detection:** ১৫ সেকেন্ড অন্তর ক্লায়েন্ট পিং এবং ৮ সেকেন্ড পং টাইমআউট দিয়ে ডেড সকেট সনাক্তকরণ ও অটো-রিকানেক্ট।
- [x] **Safe Clean Reconnect:** রিকানেক্ট কাউন্টার ডাবল-কাউন্টিং দূর করা এবং `sessionStorage`-এ `session_id` সংরক্ষণ।
- [x] **Quick Chips Prompt Clearance:** কুইক কমান্ড বাটনে `\x15` (Ctrl+U) প্রিফিক্স যুক্ত করা হয়েছে যাতে প্রম্পটের আগের অসম্পূর্ণ টেক্সট মুছে ফ্রেশ কমান্ড রান হয়।

### 4. Security Hardening (Audit 28–31)
- [x] **Cross-Site WebSocket Hijacking (CSWSH) Prevention:** ব্যাকএন্ডে `Origin` হেডার কঠোরভাবে যাচাই করা হয় (`Host`, `localhost`, `127.0.0.1`, এবং `.masud-rana.me`), অবৈধ অরিজিন থেকে সংযোগ HTTP 403 Forbidden দ্বারা প্রত্যাখ্যাত।
- [x] **Killswitch Enforcement:** টার্মিনাল ডিকনফিগার বা ডিসেবল হলে লাইভ সমস্ত PTY সেশন সাথে সাথে কিল করা হয়।
- [x] **24-Hour Connection Duration Ceiling:** যেকোনো সেশনের সর্বোচ্চ আয়ু ২৪ ঘণ্টা পর্যন্ত সীমাবদ্ধ।
- [x] **Strict HTTP-Only Device Binding:** কুকি হাইজ্যাকিং প্রতিরোধে ডিভাইস ফিঙ্গারপ্রিন্ট বাইন্ডিং ও কুয়েরি টোকেন লিকেজ প্রটেকশন।
